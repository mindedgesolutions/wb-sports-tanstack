import { titles } from '@/constants';

const SpaPlayerAchievements = () => {
  document.title = `Players Achievements | ${titles.APP_TITLE_SPORTS}`;

  return <div>SpaPlayerAchievements</div>;
};
export default SpaPlayerAchievements;

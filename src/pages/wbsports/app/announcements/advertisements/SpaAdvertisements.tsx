import { titles } from '@/constants';

const SpaAdvertisements = () => {
  document.title = `Advertisements | ${titles.APP_TITLE_SPORTS}`;

  return <div>SpaAdvertisements</div>;
};
export default SpaAdvertisements;

import { titles } from '@/constants';

const SpaAdvisoryWorking = () => {
  document.title = `Add / Edit Advisory Board / Working Committee | ${titles.APP_TITLE_SPORTS}`;

  return <div>SpaAdvisoryWorking</div>;
};
export default SpaAdvisoryWorking;
